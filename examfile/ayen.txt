#define _CRT_SECURE_NO_WARNINGS   
#include <stdio.h>
#include <string.h>    
struct Car {
    char year[20];
    char carnames[20];
    char department[50];
    char name[20];
    char num[20];
    char address[50];
};

int main()
{
    struct Car p1[] = { {"2012", "09", "아반떼" ,"이동욱" ,"010-1111-1111", "서울시 구로구 고척동"},
                        {"2020", "04", "포르쉐 911", "김석진", "010-1122-1121", "미국 네바다주 라스베가스"}};

    int i;
    for (i = 0; i < 2; i++)
        printf("%s %s %s %s %s %s\n", p1[i].year, p1[i].carnames,
            p1[i].department ,p1[i].name, p1[i].num, p1[i].address);

    return 0;
}